/**
 * Read Card Block - Save Component
 *
 * Dynamic block — server-side rendering lives in `render.php`.
 *
 * @package
 */

/**
 * @return {null} Null because this block uses server-side rendering.
 */
export default function Save() {
	return null;
}
