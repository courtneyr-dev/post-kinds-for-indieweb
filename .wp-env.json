{
	"$schema": "https://schemas.wp.org/trunk/wp-env.json",
	"core": "WordPress/WordPress#7.0",
	"phpVersion": "8.2",
	"plugins": [
		"."
	],
	"themes": [],
	"config": {
		"WP_DEBUG": true,
		"WP_DEBUG_LOG": true,
		"SCRIPT_DEBUG": true,
		"WP_ENVIRONMENT_TYPE": "local"
	},
	"mappings": {
		"wp-content/uploads": "./tests/uploads",
		"wp-content/mu-plugins": "./tests/env"
	},
	"lifecycleScripts": {
		"afterStart": "wp-env run cli wp rewrite structure '/%postname%/' --hard && (wp-env run cli wp plugin install indieauth --version=4.6.0 || true) && (wp-env run cli wp plugin install micropub --version=2.4.0 || true) && wp-env run cli wp plugin activate indieauth micropub"
	}
}
